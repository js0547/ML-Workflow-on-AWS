# Dependencies for the SageMaker SKLearn 1.2-1 training container (Python 3.9).
# The container has numpy 1.24.x pre-installed with pre-compiled C extensions.
# If numpy gets upgraded to 2.x, ALL compiled extensions break.
# CRITICAL: numpy<2.0 MUST be listed FIRST to prevent any other package
# from upgrading it.

numpy<2.0.0
xgboost>=2.0.0,<2.1.0
lightgbm>=4.1.0,<4.4.0
matplotlib>=3.7.0,<3.9.0
seaborn>=0.12.0,<0.14.0
