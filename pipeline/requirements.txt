# Dependencies for the SageMaker SKLearn 1.2-1 training container.
# ONLY list packages NOT already in the container.
# The container already has: scikit-learn, pandas, numpy, scipy, joblib
# Do NOT try to upgrade them -- it causes conflicts and crashes.

xgboost>=2.0.0,<2.2.0
lightgbm>=4.1.0,<4.5.0
matplotlib>=3.7.0
seaborn>=0.12.0
