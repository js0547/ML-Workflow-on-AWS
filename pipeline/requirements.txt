# Dependencies for the SageMaker SKLearn 1.2-1 training container (Python 3.9).
#
# The container ALREADY has: scikit-learn, pandas, numpy, matplotlib, scipy, joblib
# DO NOT install matplotlib, numpy, or seaborn here -- it breaks pre-compiled extensions.
#
# XGBoost and LightGBM are only imported when the user selects those models.
# For sklearn models (logistic_regression, linear_regression), nothing extra is needed.
# If the user picks xgboost/lightgbm and it's not installed, the script will error
# with a clear message rather than a cryptic numpy crash.
