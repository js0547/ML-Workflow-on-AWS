# Dependencies for the SageMaker SKLearn 1.2-1 training container (Python 3.9).
# The container already has: scikit-learn 1.2, pandas, numpy 1.24, scipy, joblib
# IMPORTANT: matplotlib 3.9+ requires numpy 2.x which breaks the container.
# Keep matplotlib < 3.9 to stay compatible with the pre-installed numpy 1.24.

xgboost>=2.0.0,<2.2.0
lightgbm>=4.1.0,<4.5.0
matplotlib>=3.7.0,<3.9.0
seaborn>=0.12.0,<0.14.0
